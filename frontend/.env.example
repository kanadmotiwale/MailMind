# In production, set this to your Railway backend URL (include /api)
# e.g. https://mailmind-backend-production.up.railway.app/api
# Leave unset in local dev — Vite's proxy handles it automatically.
VITE_API_URL=
